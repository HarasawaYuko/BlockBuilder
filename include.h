#pragma once
#include "DxLib.h"
#include <vector>
#include <string>
#include <cassert>
#include <float.h>

const constexpr int WIN_SIZE_X = 800;
const constexpr int WIN_SIZE_Y = 600;
const constexpr int FPS = 30;